{
  "scripts": {
    "lint": "lefthook run pre-commit",
    "setup": "npm ci --ignore-scripts && npm rebuild lefthook --ignore-scripts=false && npx lefthook install --force",
    "setup:fresh": "npm install --ignore-scripts && npm rebuild lefthook --ignore-scripts=false && npx lefthook install --force"
  },
  "packageManager": "npm@11.14.1",
  "engines": {
    "node": ">=v26.5.0 <27",
    "npm": ">=11.12.1 <12"
  },
  "devDependencies": {
    "@commitlint/cli": "=17.8.1",
    "@commitlint/config-conventional": "=17.8.1",
    "@evilmartians/lefthook": "=1.13.6",
    "prettier": "=3.9.6"
  },
  "version": "1.3.2"
}
