node_modules
site
.DS_Store
*local*.md
*.local.*
.database.env
*.csv
storybook-static
.serena
.worktrees
.claude/scheduled_tasks.lock

# Local DB dumps + secrets (never commit)
*.pgdump
app_dump*
.agents
.codex

# Playwright MCP scratch artifacts
.playwright-mcp/
planner-*.png

# Secrets: any .env at any depth (the .env.example templates stay tracked)
.env
.env.*
!.env.example
