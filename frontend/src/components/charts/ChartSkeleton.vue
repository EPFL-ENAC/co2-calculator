<template>
  <!-- Placeholder shown while an async chart's ECharts-heavy chunk loads, sized
       to the chart height so the layout doesn't jump when it swaps in. -->
  <q-skeleton type="rect" height="360px" class="full-width" />
</template>
